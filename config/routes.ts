export const authRoutes = [
  "/login",
  "/sign-up",
  "/forgot-password",
  "/reset-password",
  "/verify-email",
];

export const userRoutes = ["/settings"];

export const protectedRoutes = ["/chat"];

export const publicRoutes = [];
export const REDIRECT_DEFAULT_AUTHENTICATED_ROUTE = "/chat";
